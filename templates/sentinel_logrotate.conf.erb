# !!! generated by puppet !!!

# Rotate redis sentinel log with standard logrotate.

<%= @sentinel_log_dir %>/redis-sentinel_<%= @sentinel_name %>.log {
 weekly
 missingok
 copytruncate
 compress
 notifempty
 rotate 12
}
